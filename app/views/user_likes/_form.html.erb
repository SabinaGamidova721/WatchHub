<%= form_with(model: user_like) do |form| %>
  <% if user_like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_like.errors.count, "error") %> prohibited this user_like from being saved:</h2>

      <ul>
        <% user_like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_profile_id, style: "display: block" %>
    <%= form.text_field :user_profile_id %>
  </div>

  <div>
    <%= form.label :film_id, style: "display: block" %>
    <%= form.text_field :film_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
