<%= form_with(model: cast_film) do |form| %>
  <% if cast_film.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cast_film.errors.count, "error") %> prohibited this cast_film from being saved:</h2>

      <ul>
        <% cast_film.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cast_id, style: "display: block" %>
    <%= form.text_field :cast_id %>
  </div>

  <div>
    <%= form.label :film_id, style: "display: block" %>
    <%= form.text_field :film_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
