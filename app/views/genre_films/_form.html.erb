<%= form_with(model: genre_film) do |form| %>
  <% if genre_film.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(genre_film.errors.count, "error") %> prohibited this genre_film from being saved:</h2>

      <ul>
        <% genre_film.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :genre_id, style: "display: block" %>
    <%= form.text_field :genre_id %>
  </div>

  <div>
    <%= form.label :film_id, style: "display: block" %>
    <%= form.text_field :film_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
